# `adapter-agui` — placeholder

No code. This directory expresses an architectural boundary reserved for
milestone M5 (evidence-led extensions).

It is not published to npm, and it is not part of the default dependency graph.
An adapter lands here only when a real integrator needs it and it passes the
renderer or adapter conformance suite in `packages/testkit` — not to widen a
feature matrix.
