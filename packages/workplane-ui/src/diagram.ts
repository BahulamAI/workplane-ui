/** Mermaid diagram adapter. Requires the optional `mermaid` peer dependency.
 *  Kept on its own entry point so a host that never uses diagrams never loads it. */
export { diagramRenderer } from "./react/blocks/diagram.js";
