# `custom-renderer` — placeholder

Not written yet. The working example is [`../generative-bi`](../generative-bi),
which covers the M1 acceptance loop end to end.

A worked custom renderer already exists in the generative-BI example:
[`generative-bi/src/scenario-renderer.tsx`](../generative-bi/src/scenario-renderer.tsx).
It lives in the application, not the library, which is the point — the
extension point works from outside the repository.
